{
  "kind": "mcp-tools",
  "snapshot_version": 1,
  "tools": [
    {
      "annotations": {
        "readOnlyHint": true
      },
      "description": "Search notes by exact phrase.",
      "extra": {},
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "limit": {
            "maximum": 100,
            "minimum": 1,
            "type": "integer"
          },
          "query": {
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "query"
        ],
        "type": "object"
      },
      "name": "search_notes",
      "outputSchema": {
        "properties": {
          "hits": {
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "required": [
          "hits"
        ],
        "type": "object"
      },
      "title": "Search notes"
    }
  ]
}
